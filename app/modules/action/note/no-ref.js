import 'validate';

import Module from 'module';
export default class extends Module {
	templateUrl(){ return 'action/note/no-ref'; }
    getData(){
        return [
			
        ];
    }
    domReady(){

    }
};
