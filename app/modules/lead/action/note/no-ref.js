import 'validate';

import Module from 'module';
export default class extends Module {
	templateUrl(){ return 'lead/action/note/no-ref'; }
    getData(){
        return [
			
        ];
    }
    domReady(){

    }
};
